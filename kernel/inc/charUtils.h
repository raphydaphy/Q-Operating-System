#ifndef CHAR_UTILS_H
#define CHAR_UTILS_H

#include "intTypeDefs.h"

bool isspace(char);

bool isnum(char);

bool isalpha(char);

bool islower(char);

bool isupper(char);

char toLowerC(char);

char toUpperC(char);

int ctoi(char);

char itoc(int);

#endif
